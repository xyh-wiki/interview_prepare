1NF: 要求数据库的每一列都是不可分割的原子数据项
2NF: 在 1NF 的基础上, 非码属性必须完全依赖于候选码(在 1NF)的基础上消除非主属性对主码的部分函数依赖)
    第二范式需要确保数据库的每一列都和主键相关, 而不能只与主键的某一部分相关
3NF: 在 2NF 的基础上, 任何非主属性不依赖于其他非主属性(在 2NF的基础上消除传递依赖)
    第三范式需要确保数据表中的每一列数据都和主键直接相关, 而不能间接相关